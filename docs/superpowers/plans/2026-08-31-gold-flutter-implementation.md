# Gold Flutter 0.1.0 Implementation Plan

The release is implemented test-first on `feat/initial-generator` and merged
only after every generated variant passes analysis and tests.

1. Guard the standalone repository and public GitHub identity.
2. Validate immutable project answers.
3. Build the conditional interactive wizard and automation flags.
4. Add prerequisite checks and collision-safe staging.
5. Invoke `flutter create` and apply the exact cross-platform identity.
6. Render the Riverpod 3, AutoRoute, theme, assets, tests, and layered base app.
7. Generate a project-local development skill.
8. Add optional API services, typed failures, remotes, and repositories.
9. Add optional authentication and coordinated token refresh.
10. Add the optional complete API example from remote to presentation.
11. Verify base, API, auth, and sample variants.
12. publish installation docs, merge the feature branch, and tag `0.1.0`.

Before every commit, push, or tag, verify the repository root is exactly
`/Users/user/Documents/Projects/gold-flutter` and the remote is exactly
`https://github.com/crisantus/gold-flutter.git`.


name: gold_flutter
description: Portable gold-standard Flutter project generator.
version: 0.2.0-dev
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

executables:
  gold_flutter: gold_flutter

dependencies:
  analyzer: ^7.7.1
  args: ^2.7.0
  path: ^1.9.1
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
